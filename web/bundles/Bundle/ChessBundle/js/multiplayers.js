
$(document).ready(function(){
/*
	$('.multiplayer').click(function(slug){
			console.log(slug);
			//e.stopPropagation();
			//e.preventDefault();
			var slug = $(this).attr('id');
			var slugResult = slug.split('-');
			console.log(slugResult);
			$.post('multiplayer', { 'slugResult[]': [slugResult[0], slugResult[1]] }, function(data){
				$('body').html(data);
			})
	});
*/
});
